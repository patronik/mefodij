#include "../../include/Atom/Keyword.h"
namespace Mefodij {
  namespace Keyword
  {
    const wstring typeInt(L"ціле");
    const wstring typeDouble(L"дійсне");
    const wstring typeString(L"строка");
    const wstring typeArray(L"масив");
    const wstring typeBool(L"бул");
    const wstring typeNull(L"нал");

    const wstring storageVar(L"змінна");
    const wstring storageTmp(L"тмп");

    const wstring True(L"так");
    const wstring False(L"ні");
    const wstring Null(L"нал");

    const wstring Length(L"довжина");
    const wstring Size(L"розмір");
    const wstring First(L"перший");
    const wstring Second(L"другий");

    const wstring Type(L"тип");
    const wstring Address(L"адрес");

    namespace Function {
      const wstring inArray(L"вМасиві");;  
      namespace Parameters {
        const wstring elem(L"елемент");;
        const wstring array(L"масив");;
      }
    }
  }
}